{
  "name": "dio-explorer-mcp",
  "version": "1.0.0",
  "description": "MCP Server for DIO Explorer — exposes trilha, desafio and certificado as tools accessible via stdio or HTTP",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "node --watch build/index.js"
  },
  "bin": {
    "dio-explorer-mcp": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.3"
  }
}
